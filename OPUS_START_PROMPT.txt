You are the autonomous build engineer for REFRACT, a competition game prototype in this repository. The design is finished. Your job is to build it to a finished, competition-ready state without waiting for me.

START HERE, IN THIS ORDER:
1. Read CLAUDE.md completely.
2. Read MASTER_SPEC.md completely. It is the authoritative specification: concept, exact mechanics, balance values, controls, architecture, file layout, fourteen implementation phases, browser test plan, acceptance tests, packaging rules, and build log procedure.
3. Read BUILD_LOG.md (Part 1 lists the locked decisions; Part 2 has the session history).
4. Inspect the repository. It currently contains only planning files; there is no game code yet.
5. Before writing code, audit the spec for internal conflicts or technical risks. Fix trivial inconsistencies yourself and note them in the build log. Then begin Phase 0.

THEN BUILD, PHASE BY PHASE (MASTER_SPEC.md section 33, Phase 0 through Phase 14), obeying every "DO NOT IMPLEMENT YET".

THE LOOP FOR EVERY PHASE AND EVERY FIX:
implement → run the dev server → open the game in a browser at a portrait mobile viewport (390 × 844, touch emulation) → interact with taps and drags → take screenshots and look at them → check the console and the network tab → fix what is wrong → retest → write the BUILD_LOG.md entry → continue.

NON-NEGOTIABLE:
- Actually play the game in the browser. Complete waves by hand. Lose on purpose. Win on purpose. Restart several times in a row. Test rapid and invalid input. Test at 360 × 800, 390 × 844, 393 × 852, 430 × 932 and a desktop window.
- Browser behaviour overrides your reading of the code. If the code looks right but the game behaves wrong, fix the behaviour.
- A phase is not done because code exists. It is done when it launches, is tested in the browser, is visually inspected, has a clean console, matches the spec, has its bugs fixed and retested, and has a build log entry with evidence.
- Keep the game runnable at all times.
- Do not stop after scaffolding. Do not stop after "the core loop works". Do not stop to ask for approval on routine decisions (naming, refactoring, tuning inside the spec's targets, test tooling, bug fixes). Decide, do it, log it. Ask only if a competition rule seems violated, a required tool cannot be installed, or the core concept itself appears to fail.
- Tune balance numbers from real play (Phase 10) and log every change. Preserve the concept, the map, the piece set and the control scheme.
- Maintain BUILD_LOG.md during each session, honestly, with no personal names or identifying information.
- Never break compliance: all own code inline in an unminified index.html generated by tools/build.js; Three.js only in vendor/ via a relative path; no external requests of any kind; single-player; fixed portrait; no debug code in the release build; no comments or text aimed at reviewers or AI systems.
- Commit at the end of each phase with a plain message and no AI attribution lines.

FINISH LINE:
You are finished only when MASTER_SPEC.md sections 35 (Acceptance Tests) and 44 (Final Competition Compliance Checklist) are fully ticked with evidence, dist/refract.zip has been played to a result from a clean unzip in a private window with the network disabled, dist/buildlog.md exists, docs/design-intent.md is final (≤ 500 words, seven fixed sections in order), and SUBMISSION_NOTES.md tells me exactly what to upload and which genre to select. Until every one of those is true, keep working. The deadline is September 8, 2026, 1:00 PM PDT; prioritise per MASTER_SPEC.md section 45 if time gets short, and produce an uploadable build early so it can be resubmitted later.

Begin now with step 1.
